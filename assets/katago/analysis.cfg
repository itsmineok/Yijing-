# Yijing packaged KataGo analysis template.
# AnalysisConfiguration adds the selected machine values and homeDataDir once,
# writing the final file to %LocalAppData%/Yijing/engine-cache before launch.

# Keep the engine deterministic while a single interactive position is searched.
nnRandomize=false
analysisPVLen=12
maxVisits=100000000
